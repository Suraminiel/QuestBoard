@model List<QuestBoard.Models.Domain.JobTask>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<div class="py-2 mb-5" id="siteHeader">
    <div class="container">
        <h2>Open Tasks</h2>
    </div>
</div>

<div class="container">
    @if(Model != null && Model.Any())
    {
        <table class="table table-dark">
            <thead>
                <tr>
                   
                    <td>Name</td>
                    <td>Tags</td>
                    <td>Assigned Workers</td>
                    <td>Progress</td>
                    <td>Priority</td>
                    <td></td>
                </tr>
            </thead>
            <tbody>
                @foreach(var JobTask in Model)
                {
                    <tr>
                        
                        <td>@JobTask.Name</td>
                        <td>
                            <div class="d-flex">
                                @foreach(var tag in JobTask.Tags)
                                {
                                    <span class="badge bg-body-secondary text-dark me-1">@tag.Name</span>
                                }
                            </div>
                        </td>
                        <td>Worker 1</td>
                        <td>X/X</td>

                        <td>High</td>

                        <td>
                            <a asp-area=""
                               asp-controller="Quest"
                               asp-action="Edit"
                               asp-route-id="@JobTask.Id">
                                Open
                            </a>
                        </td>
                    </tr>
                }
               
            </tbody>

        </table>
    }
    else
    {
        <p>nothing found!</p>
    }

   
</div>